name: Python build and release

on:
  push:
    tags:
      - 'v*'
  pull_request:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release Tag'
        required: true
        default: 'v0.0.0'

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}

    if: ${{ !contains(github.event.head_commit.message, 'dev') }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            goos: linux
            goarch: amd64
            artifact_name: PalworldServerConfigParser-linux-amd64
          - os: ubuntu-22.04-arm64
            goos: linux
            goarch: arm64
            artifact_name: PalworldServerConfigParser-linux-arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
            artifact_name: PalworldServerConfigParser-windows-amd64.exe
          - os: windows-11-arm64
            goos: windows
            goarch: arm64
            artifact_name: PalworldServerConfigParser-windows-arm64.exe

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Build binary
        run: pyinstaller --onefile --name PalworldServerConfigParser main.py

      - name: Verify build output (Linux)
        if: runner.os == 'Linux'
        run: |
          if [ ! -f "dist/PalworldServerConfigParser" ]; then
            echo "Build output is missing!"
            exit 1
          fi
          chmod 755 dist/PalworldServerConfigParser
          echo "Build completed successfully."

      - name: Verify build output (Windows)
        if: runner.os == 'Windows'
        run: |
          if (!(Test-Path "dist\PalworldServerConfigParser.exe")) {
            Write-Error "Build output is missing!"
            exit 1
          }
          Write-Output "Build completed successfully."

      - name: Rename artifact (Linux)
        if: runner.os == 'Linux'
        run: |
          mv dist/PalworldServerConfigParser "dist/${{ matrix.artifact_name }}"

      - name: Rename artifact (Windows)
        if: runner.os == 'Windows'
        run: |
          Move-Item "dist\PalworldServerConfigParser.exe" "dist\${{ matrix.artifact_name }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}
          retention-days: 5

  generate-checksum:
    permissions: write-all
    name: Generate checksum file
    runs-on: ubuntu-22.04
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download dist artifact
        uses: actions/download-artifact@v6
        with:
          name: "PalworldServerConfigParser-linux-amd64"
          path: "dist"

      - name: Download dist artifact
        uses: actions/download-artifact@v6
        with:
          name: "PalworldServerConfigParser-linux-arm64"
          path: "dist"

      - name: Download dist artifact
        uses: actions/download-artifact@v6
        with:
          name: "PalworldServerConfigParser-windows-amd64.exe"
          path: "dist"

      - name: Download dist artifact
        uses: actions/download-artifact@v6
        with:
          name: "PalworldServerConfigParser-windows-arm64.exe"
          path: "dist"

      - name: Generate checksum.txt
        run: |
          cd dist
          sha256sum * > checksum.txt
          cd ..

      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Upload release artifacts
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ steps.generate-token.outputs.token }}
          file: dist/*
          tag: ${{ github.ref_name || inputs.tag_name }}
          overwrite: false
          file_glob: true
